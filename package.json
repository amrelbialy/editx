{
  "name": "editx",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean",
    "test": "turbo run test",
    "test:coverage": "turbo run test -- --coverage",
    "test:e2e": "turbo run test:e2e",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check --write .",
    "check:ci": "biome ci .",
    "dashboard": "node scripts/dashboard.mjs",
    "release": "pnpm check:ci && pnpm test && pnpm build && lerna version --conventional-commits --conventional-prerelease --yes",
    "release:graduate": "pnpm check:ci && pnpm test && pnpm build && lerna version --conventional-commits --conventional-graduate --yes",
    "publish:packages": "lerna publish from-package --dist-tag alpha --yes",
    "publish:packages:stable": "lerna publish from-package --dist-tag latest --yes",
    "release:publish": "pnpm release && pnpm publish:packages",
    "release:publish:graduate": "pnpm release:graduate && pnpm publish:packages:stable",
    "deploy:demo": "vercel --prod"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "@lerna-lite/changed": "^5.4.2",
    "@lerna-lite/cli": "^5.4.2",
    "@lerna-lite/list": "^5.4.2",
    "@lerna-lite/publish": "^5.4.2",
    "@lerna-lite/version": "^5.4.2",
    "@playwright/experimental-ct-react": "^1.58.2",
    "@playwright/test": "^1.58.2",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@vitest/coverage-v8": "^4.0.18",
    "conventional-changelog-conventionalcommits": "^9.3.1",
    "happy-dom": "^20.7.0",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@10.23.0",
  "pnpm": {
    "overrides": {
      "@sigstore/sign": "4.1.0"
    }
  },
  "engines": {
    "node": "^22.17.0 || >=24.0.0"
  }
}
